// Basic comparisons
// console.log(2 > 1);     // Logs: true (2 is greater than 1)
// console.log(2 >= 1);    // Logs: true (2 is greater than or equal to 1)
// console.log(2 < 1);     // Logs: false (2 is not less than 1)
// console.log(2 == 1);    // Logs: false (2 is not equal to 1)
// console.log(2 != 1);    // Logs: true (2 is not equal to 1)

// Comparisons with type coercion
// console.log("2" > 1);   // Logs: true (string "2" is coerced to number 2, so 2 > 1)
// console.log("02" > 1);  // Logs: true (string "02" coerced to number 2, so 2 > 1)

// Comparisons involving `null`
console.log(null > 0);     // Logs: false (null is not greater than 0)
console.log(null == 0);    // Logs: fals
